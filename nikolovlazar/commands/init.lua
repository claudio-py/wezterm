local toggle_transparency =
  require 'config.nikolovlazar.commands.toggle-transparency'
-- local toggle_theme = require 'config.nikolovlazar.commands.toggle-theme'

local M = {
  toggle_transparency,
  -- toggle_theme,
}

return M
